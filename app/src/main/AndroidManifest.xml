<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="botchway.macrocodes.madlibs">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo2"
        android:supportsRtl="true"
        android:theme="@style/ActionBar.AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StoriesListActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".InputAnswersActivity" />
        <activity android:name=".ShowResultActivity" />
        <activity android:name=".SavedStoriesActivity"></activity>
    </application>

</manifest>